- change the jump logic so it's not so restricted while under a ceiling
- add a "tether" physics contact which binds the character to arc in a fairly stiff radius around an anchor point. 


- When jump is pressed while coming out from under a ledge, the player should end up jumping up and out, similar to a wall jump, instead of bumping their head. When jump is pressed and the player is fully covered by a ledge, they should simply not jump, meaning that we can implement this behavior by adding a check for initializing jump which prevents it when a ceiling is blocking, but allowing an under-ledge jump to be buffered, with a little bit of special handling to prevent head hitting. 
- allow wall jump only when wall is present near characters hypothetical feet (lower left or right)
- activate parkourstate only for one block vaults. for two block, create a similar state that includes an initial hop to provide vertical acceleration, and 

- make jump higher priority than wall jump, unless player is pressing out or in to wall, so that it's possible to jump parallel to wall. 
- make vault require left/right dir to be pressed into direction of vault to activate/continue 
- make player crouch automatically to avoid wall friction when under ledge
- make duck under put player at stable height under ledge, so there isn't bobbing up and down. 
- Make ledge grab pull player to a resting height just below lip. 
- Make down arrow when player is overlooking ledge drop player into ledge grab. 


TODO later
- Add comments to all movementstates summarizing requirements
- Bezier curves for vault, ledge pull, and duck, which initialize arc from player position and velocity and conclude arc at target point, under constraint of avoiding corner. Try to do this in a pragmatic, configurable way. Player should be forced on Bezier path through forces strongly proportional to devation from expected pos/vel, with a capped maximum. 
- modify vault, ledge pull, duck under, etc to maintain momentum and keep movement snappy feeling, but also interruptable. 
- Integrate controller buffer parsing into movement more deeply, so that controller inputs can change movement state priority, determine which of several possible transitions is applied, etc. For instance, wall jump should only be executed after an initial jump if the jump button was recently tapped down and the down-tap didn't induce an initial jump. Or low ledge vault should be possible from either standing or running, but it should only be applied if up arrow is pressed as well as left or right pressed in direction of wall when the player is standing. However, if the player is running it should activate simply when left or right arrow is pressed into wall. 

- Track forces exterted on PhysicsContacts by movement logic (and anything else)
- Allow a max force to be specified for PhysicsContacts, exceeding of which resulting in removal
- Implement roll, ledge, overcrop, ceiling, guard, and dodge, moves. 
- Implement principled quality of life moves, such as ceiling sweep to avoid running into a ceiling corner when jumping up. 

- Implement block creation on right-click 
- Implement moving surfaces and integrate into block creation, so players can do things like sweep themselves up by creating blocks underneath them, and use the velocity from this for a really high jump. 
- Implement resource tracking to limit block creation rate. 
- Implement logic for block creation limits. (Block creation must begin within certain radius of player, but can extend slightly out of this radius after initialization as long as it's continuously active.)
- Implement actions in addition to movements. Slashes, stabs, mana stuff, etc. 
- Add semi-procedural animations. 