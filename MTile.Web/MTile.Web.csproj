<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>disable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <AssemblyName>MTile.Web</AssemblyName>
    <RootNamespace>MTile.Web</RootNamespace>
    <RollForward>Major</RollForward>
    <!-- KNI conventions, matched to the official KNI WebGLxnaProj sample. -->
    <DefineConstants>$(DefineConstants);BLAZORGL</DefineConstants>
    <KniPlatform>BlazorGL</KniPlatform>
    <BlazorEnableTimeZoneSupport>false</BlazorEnableTimeZoneSupport>
    <!-- AOT massively speeds up the physics loop in-browser but ~2-3x's the binary.
         Defer until Phase 4 perf tuning. -->
    <RunAOTCompilation>false</RunAOTCompilation>
    <!-- KNI's builder targets default to Tools\MGCB.exe, which is Windows-only
         (and the wrong case for Linux). Route through the launcher script, which
         runs the managed MGCB.dll under dotnet; see kni-mgcb.sh for the native
         libs the .fx compile needs on a fresh Linux box. -->
    <KniContentBuilderExe Condition="!$([MSBuild]::IsOSPlatform('Windows'))">$(MSBuildThisFileDirectory)kni-mgcb.sh</KniContentBuilderExe>
  </PropertyGroup>

  <ItemGroup>
    <!-- Same game source as MTile.Core, recompiled here against the KNI/Blazor
         MonoGame variant. We don't ProjectReference MTile.Core because that DLL
         is bound to MonoGame.Framework.DesktopGL — different assembly identity
         from KNI's nkast.Xna.Framework. Simpler to compile twice than to
         multi-target the library. -->
    <Compile Include="..\**\*.cs"
             Exclude="..\MTile.Tests\**;..\MTile.Desktop\**;..\MTile.Web\**;..\MTile.Rtc\**;..\MTile.Demo\**;..\MTile.Probe\**;..\MTile.FxLab\**;..\MTile.Bench\**;..\**\obj\**;..\**\bin\**;..\.claude\**" />
  </ItemGroup>

  <ItemGroup>
    <!-- KNI 4.1.9001 package set, mirroring the WebGLxnaProj sample csproj.
         Bump together when upgrading KNI — they ship as a matched set.

         PINNED, not floating. A "4.1.9001.*" float silently re-resolves on any restore;
         when that bit MonoGame on the desktop side, Core compiled against the new version
         while a host still copied the old dll and the game died at runtime with a
         FileNotFoundException. Same hazard here, plus a worse one: these twelve are a
         MATCHED SET, and a float can bump them independently (Graphics is already a
         revision ahead of its siblings), so a partial bump would mix incompatible KNI
         assemblies. Bump all twelve together, deliberately. -->
    <PackageReference Include="nkast.Xna.Framework"                          Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Content"                  Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Graphics"                 Version="4.1.9001.1" />
    <PackageReference Include="nkast.Xna.Framework.Audio"                    Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Media"                    Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Input"                    Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Game"                     Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Devices"                  Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Storage"                  Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.XR"                       Version="4.1.9001" />
    <PackageReference Include="nkast.Kni.Platform.Blazor.GL"                 Version="4.1.9001" />
    <PackageReference Include="nkast.Xna.Framework.Content.Pipeline.Builder" Version="4.1.9001" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly"           Version="8.0.11" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="8.0.11" PrivateAssets="all" />
  </ItemGroup>

  <!-- Stage game assets into source wwwroot/ so the Blazor WASM dev server picks
       them up via its static-web-asset glob. `<Content Include ... Link="wwwroot\...">`
       was tried first but only populated bin/wwwroot, which the dev server doesn't
       serve. The copied files are gitignored. -->
  <Target Name="StageGameAssetsToWwwroot" BeforeTargets="BeforeBuild">
    <!-- The rig trees are SYNCED, not accreted: WASM loads clips from a generated
         index.json manifest, and a stale clip left behind from a pre-reorg layout is
         dead weight the manifest would never reference. Wipe first, then recopy.
         Levels/ and ReferenceClips/ stay accretive (nothing enumerates them). -->
    <RemoveDir Directories="$(MSBuildThisFileDirectory)wwwroot\SkeletonStates;$(MSBuildThisFileDirectory)wwwroot\Skeletons" />
    <Copy SourceFiles="..\configs\game_config.json"       DestinationFolder="$(MSBuildThisFileDirectory)wwwroot\configs" SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\configs\movement_config.json"   DestinationFolder="$(MSBuildThisFileDirectory)wwwroot\configs" SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\configs\anim_solver_config.json" DestinationFolder="$(MSBuildThisFileDirectory)wwwroot\configs" SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\configs\impact_profiles.json"   DestinationFolder="$(MSBuildThisFileDirectory)wwwroot\configs" SkipUnchangedFiles="true" />
    <Copy SourceFiles="..\configs\material_strengths.json" DestinationFolder="$(MSBuildThisFileDirectory)wwwroot\configs" SkipUnchangedFiles="true" />
    <ItemGroup>
      <_LevelAsset Include="..\Levels\**\*" />
      <_RefClipAsset Include="..\ReferenceClips\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_RefClipAsset)"
          DestinationFiles="@(_RefClipAsset->'$(MSBuildThisFileDirectory)wwwroot\ReferenceClips\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(_LevelAsset)"
          DestinationFiles="@(_LevelAsset->'$(MSBuildThisFileDirectory)wwwroot\Levels\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <ItemGroup>
      <!-- Raw PNGs the renderer loads outside the content pipeline (rock atlas,
           backdrop) and the sprite skins + their art. .png only — Assets/ also holds
           reference material (paper.pdf) that has no business in a web payload. -->
      <_ImageAsset         Include="..\Assets\*.png" />
      <_SpriteBindingAsset Include="..\SpriteBindings\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_ImageAsset)"
          DestinationFiles="@(_ImageAsset->'$(MSBuildThisFileDirectory)wwwroot\Assets\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(_SpriteBindingAsset)"
          DestinationFiles="@(_SpriteBindingAsset->'$(MSBuildThisFileDirectory)wwwroot\SpriteBindings\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <ItemGroup>
      <_SkeletonStateAsset Include="..\SkeletonStates\**\*" />
      <_SkeletonAsset      Include="..\Skeletons\**\*" />
    </ItemGroup>
    <Copy SourceFiles="@(_SkeletonStateAsset)"
          DestinationFiles="@(_SkeletonStateAsset->'$(MSBuildThisFileDirectory)wwwroot\SkeletonStates\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
    <Copy SourceFiles="@(_SkeletonAsset)"
          DestinationFiles="@(_SkeletonAsset->'$(MSBuildThisFileDirectory)wwwroot\Skeletons\%(RecursiveDir)%(Filename)%(Extension)')"
          SkipUnchangedFiles="true" />
  </Target>

  <!-- WASM can't enumerate a directory over HTTP, so AnimationStore.LoadAll's
       Directory.GetFiles has no browser equivalent. Emit a per-rig index.json listing
       the clip files staged above; AnimationStore.LoadAllFromManifest reads it and
       fetches each clip by name. Task-batched on %(RecursiveDir) → one manifest per
       rig subdir, e.g. wwwroot/SkeletonStates/biped/index.json = ["fall.json",...]. -->
  <Target Name="GenerateClipManifests" AfterTargets="StageGameAssetsToWwwroot">
    <ItemGroup>
      <_ClipJson Include="..\SkeletonStates\**\*.json" Exclude="..\SkeletonStates\**\index.json" />
      <!-- One item per rig subdir (batched on %(RecursiveDir)); the JSON array is built
           as metadata because WriteLinesToFile's Lines is an item parameter and can't
           take a literal-concatenated transform directly (MSB4012). -->
      <_ClipManifest Include="$(MSBuildThisFileDirectory)wwwroot\SkeletonStates\%(_ClipJson.RecursiveDir)index.json">
        <Json>[@(_ClipJson->'&quot;%(Filename)%(Extension)&quot;', ',')]</Json>
      </_ClipManifest>
    </ItemGroup>
    <WriteLinesToFile File="%(_ClipManifest.Identity)" Lines="%(_ClipManifest.Json)"
                      Overwrite="true" WriteOnlyWhenDifferent="true" />
  </Target>

  <ItemGroup>
    <!-- KNI's content builder picks up KniPlatform=BlazorGL above. Output .xnb
         files land under wwwroot/Content/ so Content.Load<T>("DebugFont") works. -->
    <KniContentReference Include="..\Content\Content.mgcb"
                         Condition="!$([MSBuild]::IsOSPlatform('OSX'))">
      <Link>Content\Content.mgcb</Link>
    </KniContentReference>
    <!-- Same shader-free content set the Mac desktop build uses: the .fx compile needs
         d3dcompiler_47.dll, which has no macOS equivalent (mgfxc is Windows-only and
         wants a Wine prefix). See the header of Content.Mac.mgcb — the only consumer of
         the two effects is the metaball dev preview, off in game_config.json, and
         Game1.LoadContent tolerates the missing .xnb.

         So a Mac-published web build ships without CapsuleSplat/MetaballComposite while a
         Windows-published one ships with them. If they ever reach the real render path,
         publish from Windows or set up Wine. -->
    <KniContentReference Include="..\Content\Content.Mac.mgcb"
                         Condition="$([MSBuild]::IsOSPlatform('OSX'))">
      <Link>Content\Content.mgcb</Link>
    </KniContentReference>
  </ItemGroup>
</Project>
